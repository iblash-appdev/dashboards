Hello, world!

<h1>Convert <%= @from_symbol %> to <%= @from_symbol %></h1>

<p>Go <a href="/">back</a></p>

<ul>
  <%= @array_of_symbols.each do |a_symbol| %>
    <% @array_of_symbols.each do |a_symbol| %>
   <li>
    <a href="/forex/<%= @from_symbol %>/<%= a_symbol %>">
       Convert 1 <%= @from_symbol %> to <%= a_symbol %>...
   </li>
  
  <% end %>

  <%= @array_of_symbols.each do |a_symbol| %>
    <% @array_of_symbols.each do |a_symbol| %>
   <li>
    <a href="/forex/<%= @from_symbol %>/<%= a_symbol %>">
       Convert 1 <%= @from_symbol %> to <%= a_symbol %>...
   </li>
  
  <% end %>
</ul>   
